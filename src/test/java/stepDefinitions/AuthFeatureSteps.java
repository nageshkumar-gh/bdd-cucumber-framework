package stepDefinitions;

import io.cucumber.java.en.And;
import io.cucumber.java.en.Given;
import io.cucumber.java.en.Then;
import io.cucumber.java.en.When;

/**
 * Step definitions for `auth.feature`.
 * <p>
 * Notes:
 * - Shared authentication steps are implemented in {@link AuthStep}.
 * - Legacy demo scenarios are implemented in {@link LoginStep} / {@link PIMStep}.
 * </p>
 */
public class AuthFeatureSteps {

    // Authentication scenarios are implemented in `AuthStep` using parameterized steps.
    // This class exists as a per-feature anchor for future module-specific steps.
}
